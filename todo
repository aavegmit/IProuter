
1. increase throughput:-
    increase num of parse threads
    writing thread using RAW sockets

2. handle ttl reaches to 0 at usRTR when dst node is local node
    start sniffing packets dst to local node and ignore them in code

3. block kernel level 'Destination Unreachable'

4. perform iperf testing to determine packets/sec and throughput

5. BADchecksum problem in ICMP UNREACHABLE

6. traceroute install on routers or nodes or deter
